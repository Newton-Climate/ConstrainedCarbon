[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecosystem-complexity"
version = "0.1.0"
description = "An inference-first approach to understanding the Carbon Cycle."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Newton Nguyen", email = "nnewton@stanford.edu" }]
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.12",
    "pandas>=2.2",
    "matplotlib>=3.8",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "black>=24.0",
    "ruff>=0.4",
    "mypy>=1.10",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.black]
line-length = 88
target-version = ["py311"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "C", "N", "T"]
ignore = [
    "E203",
    # Scientific naming conventions conflict with PEP-8 lowercase rules.
    # Variables like C12, C14, log_Q10, lambda_14C, enable_14C are domain
    # standard and defined in TECHSPEC — renaming them would reduce clarity.
    "N806",   # Variable in function should be lowercase
    "N815",   # Variable in class scope should not be mixedCase
]

[tool.ruff.lint.per-file-ignores]
# Scaffold files whose names contain numerals — unavoidable in this domain.
"src/ecosystem_complexity/tracer_14C.py" = ["N999"]
"src/ecosystem_complexity/data/parsers_14C.py" = ["N999"]
"tests/test_14C.py" = ["N999"]

[tool.mypy]
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "--strict-markers"
testpaths = ["tests"]
